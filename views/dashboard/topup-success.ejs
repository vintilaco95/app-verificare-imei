<%- include('../partials/header', { title: t('dashboard.topupSuccess.pageTitle'), user: user }) %>

<div class="dashboard-container">
  <div class="dashboard-header">
    <h1><%= t('dashboard.topupSuccess.heading') %></h1>
    <a href="/dashboard/credits" class="btn btn-primary"><%= t('dashboard.topupSuccess.actions.viewCredits') %></a>
  </div>

  <div class="success-card">
    <div class="success-icon">✅</div>
    <h2><%= t('dashboard.topupSuccess.subheading') %></h2>
    <p>
      <%= t('dashboard.topupSuccess.summary')
        .replace('{amount}', chargedAmount.toFixed(2)) %>
      <%= t(transactionRecorded ? 'dashboard.topupSuccess.status.recorded' : 'dashboard.topupSuccess.status.pending') %>
    </p>
    <div class="success-actions">
      <a href="/dashboard/credits" class="btn btn-primary"><%= t('dashboard.topupSuccess.actions.viewCredits') %></a>
      <a href="/verify/imei" class="btn btn-secondary"><%= t('dashboard.topupSuccess.actions.startCheck') %></a>
    </div>
  </div>
</div>

<%- include('../partials/footer') %>

